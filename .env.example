# Copy thành .env.local rồi điền giá trị. .env.local nằm trong .gitignore — KHÔNG điền secret vào file này.

# --- Phase 1 ---------------------------------------------------------------
# Neon: Dashboard → Connect → chọn "Pooled connection" (host có "-pooler"), giữ ?sslmode=require
# Local Docker: postgres://postgres:postgres@localhost:5432/sanve
DATABASE_URL=

# --- Phase 2 (chưa dùng) ---------------------------------------------------
# Email được phép đăng nhập, cách nhau bằng dấu phẩy
ALLOWED_EMAILS=
# Sinh bằng: npx auth secret
AUTH_SECRET=
# Google Cloud Console → APIs & Services → Credentials → OAuth client ID (Web)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
TRAVELPAYOUTS_TOKEN=
# 1 = dùng MockProvider, không gọi API thật
MOCK_PROVIDER=1
# Chuỗi ngẫu nhiên ≥ 16 ký tự, cron-job.org gửi trong header x-cron-secret
CRON_SECRET=
# Telegram: nhắn @BotFather → /newbot
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
